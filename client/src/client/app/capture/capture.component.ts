import { Component } from '@angular/core';

/**
 * This class represents the lazy loaded CaptureComponent.
 */
@Component({
  moduleId: module.id,
  selector: 'capture',
  templateUrl: 'capture.component.html',
  styleUrls: ['capture.component.css'],
})

export class CaptureComponent { }
